const initState = {
    isLoggedIn: false,
    token: 'ayl'
}

const authReducer = (state = initState, action) => {
    switch (action.type) {
    
        default:
            return state;
    }
}

export default authReducer