const initState = {
    userDate: {}
}

const useReducer = (state = initState, action) => {
    switch (action.type) {
    
        default:
            return state;
    }
}

export default useReducer